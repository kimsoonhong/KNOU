#include "2_3.h"  // 작성되어 있는 2_3.h 를 불러온다.



void main(){

int add_result,sub_result;  // 없어도 상관은 없으나 적혀있으니 적어본다.

printf("10과5를 더하면 %d이다. \n" , ADD(10,5));  // 2_3.h에 저장되어 있는 ADD함수에 10과 5를 파라미터로 넘겨주고 그 값을 표출해준다.
printf("10과5를 뺴면 %d이다. \n" , SUB(10,5));  // 2_3.h에 저장되어 있는 SUB함수에 10과 5를 파라미터로 넘겨주고 그 값을 표출해준다.

}

